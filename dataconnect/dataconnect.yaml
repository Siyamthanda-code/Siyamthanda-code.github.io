specVersion: "v1beta"
serviceId: "BITE"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "BITE-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
